/////////////////////////////////////////////////////////////////////////////
// capsense_power_modes.h
/////////////////////////////////////////////////////////////////////////////

#ifndef CAPSENSE_POWER_MODES_H_
#define CAPSENSE_POWER_MODES_H_

/////////////////////////////////////////////////////////////////////////////
// Defines
/////////////////////////////////////////////////////////////////////////////

#define PM_RESPONSIVE 0
#define PM_LOW_POWER  1

#define RESPONSIVE_SLEEP_PERIOD   20
#define RESPONSIVE_ACTIVE_PERIOD  20
#define RESPONSIVE_SLEEP_COUNTS   50

#define LOW_POWER_SLEEP_PERIOD    500
#define LOW_POWER_ACTIVE_PERIOD   20
#define LOW_POWER_SLEEP_COUNTS    100

/////////////////////////////////////////////////////////////////////////////
// Macros
/////////////////////////////////////////////////////////////////////////////

// Return true if object a intersects object b (overlaps by at least 1 px)
#define OBJ_OVERLAPS_OBJ(a, a_len, b, b_len)    (a + a_len > b && a < b + b_len)

// Return true if line a intersects object b
#define LINE_OVERLAPS_OBJ(a, b, b_len)          OBJ_OVERLAPS_OBJ(a, 1, b, b_len)

/////////////////////////////////////////////////////////////////////////////
// Prototypes
/////////////////////////////////////////////////////////////////////////////

void capsense_power_modes_init();
void capsense_power_modes_main();

#endif /* CAPSENSE_POWER_MODES_H_ */
